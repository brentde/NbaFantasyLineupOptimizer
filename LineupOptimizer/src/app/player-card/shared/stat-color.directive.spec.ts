import { StatColorDirective } from './stat-color.directive';

describe('StatColorDirective', () => {
  it('should create an instance', () => {
    const directive = new StatColorDirective();
    expect(directive).toBeTruthy();
  });
});
